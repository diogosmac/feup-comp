import static ioPlus.printHelloWorld(void) void;
import static io.printArray(int[]) void;
import static io.print(int) void;
import Math;
import Math.calculate(int, int, int[]) int;
import static Math.cenas() boolean;

class HelloWorld {
    public boolean cenas() {
        return (2 < 1) && (2 < 2);
    }

	public static void main(String[] args) {
	    boolean cenas;
	    HelloWorld world;
	    world = new HelloWorld();
	    cenas = world.cenas();

	    if (cenas) {
	        io.print(1);
	    }
	    else {
	        io.print(2);
	    }

        ioPlus.printHelloWorld();
	}
}